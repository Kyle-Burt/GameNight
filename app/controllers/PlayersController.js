import { AppState } from "../AppState.js";


export class PlayersController {
    constructor() {
        console.log('players control loaded');
        console.log(AppState.players)
    }

    // _drawPlayers() {
    //     let players = AppState.players
    //     let template = ''

    //     players.forEach(player => )
    // }
}